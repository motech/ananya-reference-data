<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-2.0.xsd">

    <changeSet context="setup" author="ThoughtWorks" id="1">
        <sql splitStatements="false">
            CREATE TABLE reference_data.location_dimension(
            id serial,
            location_id varchar(30),
            district varchar(255),
            block varchar(255),
            panchayat varchar(255)
            );
        </sql>
    </changeSet>

    <changeSet context="setup" author="ThoughtWorks" id="2">
        <sql splitStatements="false">
            CREATE TABLE reference_data.front_line_worker_dimension(
            id serial,
            msisdn bigint,
            operator varchar(30),
            name varchar(255),
            status varchar(30),
            designation varchar(30),
            circle varchar(30)
            );
        </sql>
    </changeSet>
</databaseChangeLog>